# GoTube 环境变量配置模板
# 复制为 .env 并修改相应值

# 服务端口
GOTUBE_PORT=8000

# 服务监听地址
GOTUBE_HOST=0.0.0.0

# 虚拟环境目录
GOTUBE_VENV_DIR=./venv

# 启动 PID 文件
GOTUBE_PID_FILE=./.server.pid

# 生产日志文件
GOTUBE_LOG_FILE=./server.log

# 启动 PID 文件
GOTUBE_PID_FILE=./.server.pid

# 启动时若未发现虚拟环境，是否自动创建
GOTUBE_AUTO_INIT_VENV=1

# 启动初始化时是否自动安装依赖
GOTUBE_AUTO_INSTALL_DEPS=1

# 生产启动时是否构建压缩后的前端资源
# 开启后会将 www 构建到 www_dist，并由服务端直接提供压缩产物
GOTUBE_BUILD_FRONTEND=1

# 下载主页隐藏路径
GOTUBE_HIDDEN_PATH=7777

# 最大并发下载数
GOTUBE_MAX_CONCURRENT=5

# 单用户最大同时下载数（0=不限制，默认1）
# 登录用户按账号限制，游客按浏览器会话限制
GOTUBE_MAX_DOWNLOADS_PER_USER=1

# 下载目录
GOTUBE_DOWNLOAD_DIR=./downloads

# 是否允许匿名用户下载（默认开启）
# 匿名用户下载的文件存储到临时目录，关闭页面后自动删除
GOTUBE_ALLOW_GUEST_DOWNLOAD=1

# Cookies 文件路径
# 仅作为首次导入旧 Cookie 使用；运行期 Cookie 由 data/cookies.txt 管理。
# 管理后台上传、删除 Cookie 后，下载器只使用 data/cookies.txt。
GOTUBE_COOKIES_FILE=./cookies.txt

# 服务端静态目录（生产建议使用构建产物）
GOTUBE_WWW_DIR=www_dist

# 允许跨域访问 API 的来源列表（逗号分隔，留空表示仅同源）
# 例如：http://localhost:5173,https://panel.example.com
GOTUBE_CORS_ALLOW_ORIGINS=


# 管理页面认证账号（支持多管理员，格式：用户名1:密码1,用户名2:密码2）
# 密码强度要求：至少 8 位，不得使用常见弱密码，不得与用户名相同
GOTUBE_ADMINS=admin:请改成你自己的强密码

# 调试模式（仅控制日志详情；API 文档 /docs 等始终不对外暴露）
GOTUBE_DEBUG=0

# 登录 Cookie 是否携带 Secure 标记（HTTPS/反向代理部署建议开启；纯 HTTP 访问会导致 Cookie 无法下发）
GOTUBE_COOKIE_SECURE=0

# 全局 API 速率限制：每 IP 每分钟最大请求数（0=不限制，默认 300）
GOTUBE_RATE_LIMIT=300

# ./gotube.sh upgrade 代码同步源（生产目录为纯代码部署，不保留 .git）
# fork 用户可改为自己仓库；不配置时默认官方仓库的默认分支
#GOTUBE_REPO_URL=https://github.com/Bikakko/gotube.git
#GOTUBE_BRANCH=master

# 日志级别（ERROR/WARNING/INFO/DEBUG，生产环境建议用 ERROR）
GOTUBE_LOG_LEVEL=ERROR

# 生产启动时是否自动更新 yt-dlp（0=关闭，1=开启）
GOTUBE_AUTO_UPDATE_YTDLP=0

# 是否允许播放列表/用户空间URL下载（0=禁止，1=允许）
# 禁止后仅支持单个视频链接下载
GOTUBE_ALLOW_PLAYLIST_DOWNLOAD=0

# 单个视频最大文件大小限制（单位：MB，0=不限制）
# 超过此大小的视频将被拒绝下载
GOTUBE_MAX_VIDEO_SIZE_MB=0

# 普通用户默认视频库容量限制（MB），0=不限制；管理员不受此限制
GOTUBE_USER_STORAGE_QUOTA_MB=0

# ── 数据库自动备份 ──
# 应用内定时任务，每隔 N 小时用 VACUUM INTO 在线备份 gotube.db，只保留最近 N 份
# 备份目录（相对路径按项目根解析）
GOTUBE_BACKUP_DIR=./databackups

# 备份间隔（小时）
GOTUBE_BACKUP_INTERVAL_HOURS=24

# 保留份数
GOTUBE_BACKUP_RETENTION=3
