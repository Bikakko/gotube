# GoTube 相册首页说明

`4.2.0` 起，根路径 `/` 不再直接暴露业务首页，而是展示公开可浏览的相册页。

## 目录结构

相册根目录固定为项目根目录下的：

```text
gallery/
```

仅支持一级子目录作为相册：

```text
gallery/
  memes/
    001.jpg
    002.gif
  reactions/
    a.png
    b.webp
```

规则：

- 每个一级子目录对应一个相册卡片
- 目录名作为相册标题
- 目录中第一张合法图片作为封面
- 目录中没有合法图片时，不显示该相册

## 支持的图片格式

- `.jpg`
- `.jpeg`
- `.png`
- `.webp`
- `.gif`
- `.avif`

其他格式会被忽略，不会出现在首页，也不会被接口暴露。

## 更新方式

当前版本只支持手工维护图片：

- `WinSCP`
- `scp`
- 服务器文件管理器

直接上传到 `gallery/<相册目录>/` 即可。上传完成后刷新首页即可看到新相册或新图片。

## 隐蔽入口

首页保留一个弱化的隐蔽入口，当前实现为点月亮跳转到隐藏下载页。

## 安全边界

相册读取只允许访问 `gallery/` 下一级目录中的白名单图片文件。接口已做：

- `slug` 校验
- 文件名校验
- `resolve()` 边界检查
- 非法访问返回 `404`

不要依赖二级目录或软链接组织图片，这不在当前支持范围内。
