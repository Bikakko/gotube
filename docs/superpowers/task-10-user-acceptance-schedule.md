# 任务 10 用户手工验收安排表

本文只列出需要用户手工完成的验收事项。每一步完成后，请按文末「反馈格式」把结果发给 Codex，我会根据你的反馈判断是否需要修复代码。

## 0. 验收准备

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 0.1 | 打开 PowerShell，进入项目目录：`cd D:\工作区\gotube.dev\gotube` | 当前目录正确 | 是否成功 |
| 0.2 | 确认分支：`git status --short --branch` | 显示 `codex/v4-multi-user-library...origin/codex/v4-multi-user-library`，且没有未提交文件 | 命令输出 |
| 0.3 | 启动服务：`.\st.bat` | 终端显示服务启动，访问地址为 `http://localhost:8000` | 是否成功；如失败，复制错误 |
| 0.4 | 浏览器打开 `http://localhost:8000/health` | 返回正常健康状态 | 页面返回内容 |
| 0.5 | 打开下载页 `http://localhost:8000/7777` | 页面能打开，不强制弹登录框 | 是否成功 |

建议验收时打开浏览器 DevTools，重点观察 Console、Network 和 Application 中的 localStorage / sessionStorage。

## 1. 游客基础流程

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 1.1 | 清空该站点 localStorage 和 sessionStorage，然后刷新下载页 | 页面保持游客状态，不显示用户视频库 | 是否符合 |
| 1.2 | 提交一个短视频链接下载 | 出现任务卡，进度能变化，完成后不需要刷新页面 | 视频平台、链接类型、是否需要刷新 |
| 1.3 | 点击任务卡「播放」 | 能打开播放弹窗并播放 | 是否能播放；如不能，说明按钮状态和报错 |
| 1.4 | 点击任务卡「下载」 | 能下载文件，文件名有正常后缀 | 下载文件名 |
| 1.5 | 刷新页面 | 同一游客 session 的任务不异常丢失或串到用户库 | 是否符合 |

## 2. 游客转存到普通用户

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 2.1 | 保持第 1 组游客已完成视频不变，登录普通用户 A | 页面提示游客视频转存 | toast 文案 |
| 2.2 | 查看「我的视频库」 | 刚才游客下载的视频出现在普通用户 A 视频库 | 是否出现 |
| 2.3 | 点击该视频的播放、下载、分享复制 | 播放可用，下载文件正常，分享链接可复制 | 哪些可用，哪些不可用 |
| 2.4 | 打开分享链接 | 能播放该用户分享的视频 | 分享链接是否可播放 |
| 2.5 | 退出普通用户 A | 任务卡和视频库清空，页面回到未登录状态 | 是否仍残留视频卡或用户库 |

## 3. 登录态隔离

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 3.1 | 普通用户 A 登录后退出，再登录管理员 | 下载页不显示普通用户 A 的视频库 | 是否看到 A 的库 |
| 3.2 | 管理员登录后点击管理入口 | 能进入管理后台 | 是否成功进入 |
| 3.3 | 管理员退出登录 | 不应继续停留在已授权的 `/admin` 状态 | 退出后页面位置 |
| 3.4 | 管理员退出后重新进入下载页 | 不应出现管理员或普通用户旧任务卡 | 是否出现旧卡片 |
| 3.5 | 普通用户 A 退出后登录普通用户 B | B 不应看到 A 的视频库或任务卡 | 是否串库 |

## 4. 用户视频库与容量

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 4.1 | 登录普通用户 A，下载一个新视频 | 下载完成后进入 A 的视频库，容量增加 | 容量变化 |
| 4.2 | 登录普通用户 B，下载 A 已下载过的同一链接 | 尽量复用已有媒体，但 B 拥有自己的视频库条目 | 是否重新下载；B 是否有条目 |
| 4.3 | 给普通用户 A 设置较小容量，下载一个超过剩余容量但 A 尚未满的视频 | 最后一个视频允许入库，容量达到或超过上限 | 是否入库；容量显示 |
| 4.4 | 在 A 已满或超额后继续下载新视频 | 新下载应被拒绝，提示容量不足 | 提示文案 |
| 4.5 | 删除 A 视频库中的一个视频 | 只从 A 的视频库删除；如果 B 仍拥有同一视频，B 不受影响 | A/B 结果 |

## 5. URL 复用

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 5.1 | 用户 A 下载一个 YouTube 或 Bilibili 普通链接 | 正常完成并入库 | 链接平台 |
| 5.2 | 用户 B 下载同一个视频但带播放进度参数，例如 `t=42s` | 应复用已有媒体，不应完整重新下载 | 是否重新走下载流程 |
| 5.3 | Bilibili 链接带 `vd_source` 等追踪参数重复测试 | 追踪参数不影响复用 | 是否复用 |
| 5.4 | 游客下载已存在于视频库的 URL | 尽量复用已有媒体；如果仍重新下载，请记录 | 是否复用 |

## 6. 分享链接有效性

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 6.1 | 用户 A 开启某视频分享，复制链接 | 链接包含用户级分享 token | 链接是否可播放 |
| 6.2 | 用户 A 关闭该视频分享，再打开旧链接 | 旧链接应失效 | 页面表现 |
| 6.3 | 用户 A 重新开启分享 | 新状态下分享可用 | 是否恢复 |
| 6.4 | 用户 A 删除该视频库条目 | A 的旧分享失效 | 是否失效 |
| 6.5 | 用户 B 如果也拥有同一物理视频，检查 B 的分享 | B 的分享不应受 A 删除影响 | 是否受影响 |

## 7. 管理员维护删除

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 7.1 | 管理员进入全局视频库 | 能看到 legacy 视频、用户视频或全局资产 | 是否能看到 |
| 7.2 | 找一个被 A 和 B 都拥有的视频，执行维护性删除 | 物理文件删除，A 和 B 的视频库条目都清理 | A/B 是否还显示 |
| 7.3 | 删除后再用相同 URL 下载 | 不应命中已经不存在的坏记录；应正常重新下载或重新登记 | 是否正常 |
| 7.4 | 检查原分享链接 | 所有关联分享应失效 | 是否失效 |

## 8. 下载中断与关闭页面

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 8.1 | 游客提交一个较长下载，任务进入下载中后关闭标签页 | 30 秒内不立即清理，超过 30 秒且无重连后取消下载并清理临时文件 | 是否清理；大约等待时间 |
| 8.2 | 游客提交较长下载，关闭后 30 秒内重新打开同一页面 | 不应误取消 | 是否继续 |
| 8.3 | 普通用户 A 下载中直接关闭标签页 | 下载应继续后台运行，完成后进入 A 视频库 | 是否入库 |
| 8.4 | 普通用户 A 下载中点击退出登录，选择取消下载并退出 | 任务变成已取消，残留被清理，退出成功 | 是否符合 |
| 8.5 | 普通用户 A 下载中点击退出登录，选择继续后台下载并退出 | 退出成功；下载完成后仍归属 A | 是否符合 |

## 9. 大文件与分离音视频

| 步骤 | 操作 | 预期 | 你需要反馈 |
| --- | --- | --- | --- |
| 9.1 | 设置较小的 `GOTUBE_MAX_VIDEO_SIZE_MB` 后重启服务 | 配置生效 | 设置值 |
| 9.2 | 下载一个最终文件可能超限的视频 | 如果可预估超限，应尽早拒绝；如果下载中发现超限，应停止并清理 | 何时提示超限 |
| 9.3 | 下载 YouTube / Bilibili 这类音视频分离的视频并触发超限 | 不应留下孤儿 `.m4a`、`.webm`、`.part`、`.ytdl`、`.temp` 文件 | `downloads` 下是否有残留 |
| 9.4 | 恢复正常大小限制并重启服务 | 后续验收不受小限制影响 | 是否恢复 |

## 10. 最终回报格式

完成一组就可以反馈，不必等全部完成。建议按下面格式发给 Codex：

```text
任务 10 手工验收反馈

验收组：第 X 组 - 名称
结果：通过 / 有问题 / 跳过

操作环境：
- 浏览器：
- 视频平台：
- 测试链接类型：
- 登录身份：

现象：
- 预期：
- 实际：
- 页面提示：
- Console 报错：
- Network 异常：

补充：
- 截图或录屏：
- server.log 相关片段：
```

如果某一步失败，请尽量保留当前页面，不要立刻刷新。先把反馈发给 Codex，我会根据现象决定是否需要看日志、数据库或文件目录。

