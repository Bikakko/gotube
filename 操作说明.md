# GoTube 操作说明

---

## 一、快速开始

### 1.1 访问地址

| 环境 | 地址 | 说明 |
|------|------|------|
| 本地开发 | `http://localhost:8000` | 直接访问 |
| 远程生产 | `https://v.pikakko.top/7777` | 需要认证 |

**注意**：远程访问需要先通过 Nginx Basic Auth 认证（用户名/密码均为 `7777`）。

### 1.2 启动服务

```bash
# 方式一：使用 systemd（生产环境）
systemctl start gotube

# 方式二：使用启动脚本（开发环境）
cd /root/gotube
./st.sh

# 方式三：直接启动（开发环境，热重载）
source venv/bin/activate
uvicorn server.main:app --host 0.0.0.0 --port 8000 --reload
```

### 1.3 停止服务

```bash
# 方式一：使用 systemd
systemctl stop gotube

# 方式二：使用启动脚本
./start-server.sh stop
```

---

## 二、配置修改

所有配置通过 `.env` 文件管理，修改后需要重启服务生效。

### 2.1 修改密码

修改 Nginx Basic Auth 密码：

```bash
# 1. 编辑 .env 文件
vim /root/gotube/.env

# 2. 修改以下两项
GOTUBE_AUTH_USER=你的用户名
GOTUBE_AUTH_PASS=你的密码

# 3. 生成新的密码文件（使用 htpasswd 工具）
htpasswd -c /etc/nginx/.htpasswd 你的用户名

# 4. 重启 Nginx 和 GoTube
systemctl restart nginx
systemctl restart gotube
```

**注意**：密码文件位于 `/etc/nginx/.htpasswd`，仅需修改 `.env` 中的配置即可。

### 2.2 修改下载路径

```bash
# 1. 编辑 .env 文件
vim /root/gotube/.env

# 2. 修改下载目录（建议使用绝对路径）
GOTUBE_DOWNLOAD_DIR=/data/videos

# 3. 确保目录存在且有写权限
mkdir -p /data/videos
chown root:root /data/videos
chmod 755 /data/videos

# 4. 重启服务
systemctl restart gotube
```

### 2.3 修改服务端口

```bash
# 1. 编辑 .env 文件
vim /root/gotube/.env

# 2. 修改端口
GOTUBE_PORT=8080

# 3. 修改 Nginx 配置（如果使用反向代理）
vim /etc/nginx/sites-available/gotube
# 将 proxy_pass 改为 http://localhost:8080

# 4. 重启服务
systemctl restart gotube
systemctl restart nginx
```

### 2.4 修改隐藏路径

下载主页的隐藏路径（默认 `7777`）：

```bash
# 1. 编辑 .env 文件
vim /root/gotube/.env

# 2. 修改隐藏路径
GOTUBE_HIDDEN_PATH=mysecret

# 3. 重启服务
systemctl restart gotube
```

修改后访问地址变为：`https://v.pikakko.top/mysecret`

### 2.5 修改并发数

```bash
# 1. 编辑 .env 文件
vim /root/gotube/.env

# 2. 修改并发数（1-20）
GOTUBE_MAX_CONCURRENT=10

# 3. 重启服务
systemctl restart gotube
```

---

## 三、环境切换

### 3.1 开发环境

开发环境特点：
- 热重载（代码修改自动重启）
- 启用 Swagger UI 文档（`/docs`）
- 详细日志输出

```bash
# 1. 编辑 .env 文件，开启调试模式
vim /root/gotube/.env
GOTUBE_DEBUG=1

# 2. 使用启动脚本（自动启用热重载）
cd /root/gotube
./start-server.sh

# 3. 访问 Swagger UI
# http://localhost:8000/docs
```

### 3.2 生产环境

生产环境特点：
- 禁用调试模式
- 使用 systemd 管理
- Nginx 反向代理 + HTTPS

```bash
# 1. 编辑 .env 文件，关闭调试模式
vim /root/gotube/.env
GOTUBE_DEBUG=0

# 2. 使用 systemd 启动
systemctl enable gotube
systemctl start gotube

# 3. 检查服务状态
systemctl status gotube
```

### 3.3 快速切换

```bash
# 切换到开发环境
sed -i 's/GOTUBE_DEBUG=0/GOTUBE_DEBUG=1/' /root/gotube/.env
./start-server.sh

# 切换到生产环境
sed -i 's/GOTUBE_DEBUG=1/GOTUBE_DEBUG=0/' /root/gotube/.env
systemctl restart gotube
```

---

## 四、常用操作

### 4.1 查看日志

```bash
# 实时查看日志
journalctl -u gotube -f --no-pager

# 查看最近 100 行日志
journalctl -u gotube -n 100 --no-pager

# 查看特定时间段日志
journalctl -u gotube --since "2026-04-11 10:00:00" --until "2026-04-11 12:00:00"
```

### 4.2 查看下载文件

```bash
# 列出所有下载文件
ls -la /root/gotube/downloads/

# 查看文件大小
du -sh /root/gotube/downloads/*

# 查看特定视频
ls -la /root/gotube/downloads/*关键词*
```

### 4.3 更新 yt-dlp

```bash
# 激活虚拟环境
source /root/gotube/venv/bin/activate

# 更新 yt-dlp
pip install --upgrade yt-dlp

# 验证版本
yt-dlp --version

# 重启服务
systemctl restart gotube
```

### 4.4 更新 Cookies

YouTube、B站等平台需要有效的 cookies 才能下载会员内容：

```bash
# 1. 从浏览器导出 cookies（Netscape 格式）
# 使用浏览器插件如 "Get cookies.txt LOCALLY"

# 2. 备份旧 cookies
cp /root/gotube/cookies.txt /root/gotube/cookies.txt.bak

# 3. 替换新 cookies
# 将导出的 cookies.txt 上传到 /root/gotube/cookies.txt

# 4. 重启服务
systemctl restart gotube
```

---

## 五、故障排查

### 5.1 服务无法启动

```bash
# 1. 检查端口占用
lsof -i :8000
# 或
ss -tlnp | grep 8000

# 2. 检查日志
journalctl -u gotube -n 50 --no-pager

# 3. 手动启动测试
cd /root/gotube
source venv/bin/activate
python -c "from server.main import app; print('导入成功')"
```

### 5.2 下载失败

```bash
# 1. 检查 yt-dlp 是否正常
yt-dlp --version

# 2. 测试下载
yt-dlp -v "https://www.youtube.com/watch?v=测试视频ID"

# 3. 检查 cookies
ls -la /root/gotube/cookies.txt
head -5 /root/gotube/cookies.txt

# 4. 查看应用日志
journalctl -u gotube -f --no-pager
```

### 5.3 权限问题

```bash
# 1. 检查下载目录权限
ls -la /root/gotube/downloads/

# 2. 修复权限
chmod 755 /root/gotube/downloads/

# 3. 检查日志目录权限
ls -la /var/log/journal/
```

---

## 六、完整配置示例

### 6.1 开发环境配置

```bash
# /root/gotube/.env (开发环境)
GOTUBE_PORT=8000
GOTUBE_HIDDEN_PATH=7777
GOTUBE_MAX_CONCURRENT=5
GOTUBE_DOWNLOAD_DIR=./downloads
GOTUBE_COOKIES_FILE=./cookies.txt
GOTUBE_AUTH_USER=7777
GOTUBE_AUTH_PASS=7777
GOTUBE_DEBUG=1
```

### 6.2 生产环境配置

```bash
# /root/gotube/.env (生产环境)
GOTUBE_PORT=8000
GOTUBE_HIDDEN_PATH=7777
GOTUBE_MAX_CONCURRENT=10
GOTUBE_DOWNLOAD_DIR=/data/videos
GOTUBE_COOKIES_FILE=/root/gotube/cookies.txt
GOTUBE_AUTH_USER=admin
GOTUBE_AUTH_PASS=强密码123!
GOTUBE_DEBUG=0
```

---

## 七、管理视频库页面

### 7.1 访问地址

管理入口：`https://v.pikakko.top/` (或访问隐藏路径 `/7777/admin`)

V3.0 之后，首页 `/` 已改为统一登录门户。登录成功后可进入管理页面。

### 7.2 用户管理 (V3.0+ 新增)

管理页面不再使用 `.env` 中的硬编码密码，而是采用 SQLite 数据库存储用户信息。

#### 首次启动

首次升级到 V3.0 启动时，系统会自动创建默认管理员账户：
- 用户名：`admin`
- 密码：`admin` (建议登录后立即修改)

可以通过 `.env` 配置初始账户（仅在数据库为空时生效）：
```bash
GOTUBE_INIT_ADMIN_USER=custom_admin
GOTUBE_INIT_ADMIN_PASS=your_secure_password
```

#### 多角色权限

系统支持三种角色：
1. **管理员 (admin)**：拥有全部权限，包括用户管理、视频管理、导出、删除等。
2. **普通用户 (user)**：拥有视频管理、导出、删除权限，但不能管理其他用户。
3. **只读用户 (readonly)**：仅能查看视频列表、播放、分享和查看统计，无法删除视频、修改标签或进行批量操作。

#### 用户管理操作

管理员点击导航栏的 **"👥 用户管理"** 按钮即可：
- 新增/编辑用户
- 重置用户密码
- 启用/禁用账号
- 删除用户

### 7.3 主要功能
...

#### 视频浏览与筛选

- **关键词搜索**：在搜索框输入关键词，按视频标题搜索
- **按来源筛选**：选择 YouTube、Bilibili、Twitter/X 等来源平台
- **按时间筛选**：今天、本周、本月、更早
- **按标签筛选**：输入标签名称进行筛选，支持多标签

#### 视频操作

- **播放视频**：点击视频卡片上的播放按钮或缩略图
- **分享链接**：点击分享按钮生成 `/watch?v={hash}` 链接
- **管理标签**：为视频添加或删除标签，方便分类管理
- **删除视频**：单个删除或批量删除（会删除文件及元数据）

#### 批量操作

1. 勾选多个视频（点击卡片左上角复选框）
2. 底部会出现批量操作栏
3. 可执行：导出 ZIP、导出 m3u8、批量删除

#### 导出功能

| 导出格式 | 说明 |
|----------|------|
| **ZIP** | 打包选中的视频文件、元数据和缩略图 |
| **JSON** | 导出所有视频的元数据（标题、标签、时间等） |
| **m3u8** | 生成标准 m3u8 播放列表，可用播放器直接播放 |

> **提示**：未勾选视频时执行导出，将导出**全部视频**。

#### 统计面板

点击导航栏的"统计面板"按钮，可查看：
- 视频总数和总大小
- 按来源平台分布
- 按时间分布（今天、本周等）

### 7.4 标签管理

#### 为视频添加标签

1. 点击视频卡片右下角的 `⋮` 菜单
2. 选择"管理标签"
3. 在弹出框中输入标签名称，按回车添加
4. 点击标签上的 `×` 可删除标签
5. 点击"保存"生效

#### 按标签筛选

在筛选栏的标签输入框中输入标签名称，回车即可添加筛选条件。点击标签上的 `×` 可移除该筛选。

### 7.5 Token 认证说明

- 登录成功后会生成一个随机 token，有效期 **120 小时（5 天）**
- Token 存储在浏览器 localStorage 中
- 每次访问会自动续期
- 过期后需要重新登录

### 7.6 安全注意事项

1. **修改默认密码**：部署后立即修改 `GOTUBE_ADMIN_USER` 和 `GOTUBE_ADMIN_PASS`
2. **Token 安全**：Token 保存在浏览器中，不要在公共电脑上使用
3. **路径遍历防护**：删除和导出接口会校验文件名，防止 `../` 攻击
4. **XSS 防护**：所有动态内容都经过 HTML 转义

---

## 八、安全建议

1. **修改默认密码**：首次部署后立即修改 `GOTUBE_AUTH_USER` 和 `GOTUBE_AUTH_PASS`
2. **使用强密码**：生产环境使用复杂密码，包含大小写字母、数字、特殊字符
3. **定期更新 Cookies**：YouTube/B站 cookies 有效期有限，定期更新
4. **备份重要数据**：定期备份 `downloads/` 目录和 `.env` 配置文件
5. **限制访问**：通过防火墙限制访问 IP，或使用 VPN

---

## 九、常用命令速查

| 命令 | 说明 |
|------|------|
| `systemctl start gotube` | 启动服务 |
| `systemctl stop gotube` | 停止服务 |
| `systemctl restart gotube` | 重启服务 |
| `systemctl status gotube` | 查看状态 |
| `journalctl -u gotube -f` | 查看实时日志 |
| `./start-server.sh` | 开发模式启动 |
| `./start-server.sh stop` | 开发模式停止 |
| `htpasswd -c /etc/nginx/.htpasswd 用户名` | 修改认证密码 |
| `pip install --upgrade yt-dlp` | 更新 yt-dlp |

---

*最后更新: 2026-04-12 - 新增管理视频库页面功能*
